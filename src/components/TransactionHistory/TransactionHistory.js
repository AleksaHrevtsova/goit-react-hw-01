import React from "react";
import classes from "./TransactionHistory.module.css";

const TransactionHistory = () => {
  return <h2 className={classes.title}>TransactionHistory</h2>;
};

export default TransactionHistory;
